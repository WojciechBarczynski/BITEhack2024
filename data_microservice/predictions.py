# Returns tuple (return_prob, no_return_prob)
# return_prob - probability of lung cancer if person returns to smoking
# no_return_prob - probability of lung cancer if person won't smoke ever again
def predict_lung_cancer(age: int, weight: int, height: int, cleanDays: int) -> tuple[float, float]:
    # TODO
    return 1.0, 0.5
