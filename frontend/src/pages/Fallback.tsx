import React from 'react';

const Fallback = () => {
  /*
    Responsibility:
    Redirect to either login or home (profile) depending on if user is logged in
  */

  return (
    <div>
      Fallback
    </div>
  );
};

export default Fallback;