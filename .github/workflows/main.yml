name: Does it works?

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - run: cd frontend
      - run: npm install
      #- run: npm start

      - run: cd backend
      - run: build gradle project

      - run: cd data_microservice
      - run: pip install -r requirements.txt
      #- run: python3 app.py
